# Full GPU stack (CUDA 12.4 wheels + this repo's Python deps):
#   pip install -r requirements-gpu.txt
#
# If you prefer the official one-liner for torch only, run that first, then `requirements.txt`:
#   pip install torch==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
#
# This file uses `--extra-index-url` so PyPI remains available for FastAPI/transformers/etc.

-r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
